El codigo se ejecuta en el archivo main.py
Hay que instalar las siguientes librerias:
Matplotlib.pyplot, undecoded, numpy.

No hace falta escribir ningun comando extra para el funcionamiento del programa.
Como no existe la disciplina con disciplina_id  = 0, se tomo que la disciplina_id era -1, o sea, Sin_datos como un gran area
Esto lo tomamos en cuenta para los distintos calculos requeridos
Las visualizaciones se muestran en graficos o en archivos txt, dependiendo el punto